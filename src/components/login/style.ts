import styled from'styled-components';

export const SwitchWrap = styled.div`
  display: flex;
  span {
    cursor: pointer;
    margin-right: 20px;
    color: #999;
  }
  .active {
    color: #333;
    font-weight: bold;
  }
`